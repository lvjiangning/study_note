import './main.css';
import Tab from '../tab';
import '../content';
import 'components/loading';

const tabEl = document.querySelector('.tab');
const tab = new Tab(tabEl);

// tab.setActiveItem(6);
// tab.to(0);
